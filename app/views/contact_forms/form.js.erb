
<% if @contact_form.errors.any? %>
  $("#form").html("<%= j render partial: 'contact_forms/form' %>");
  // else, render form over old form

<% else %>
  // if we sent it, replace with "SUP SUP"
  $("#form").html("<h2 class='thanks'>Thank You for Your Message.</h2>")

<% end %>
